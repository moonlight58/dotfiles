;; Import widgets
(include "./music-player.yuck")
(include "./monitor.yuck")
(include "./nothing-clock.yuck")

;; Time vars
(defpoll HOUR :interval "5s" `date +\"%H\"`)
(defpoll MIN :interval "5s" `date +\"%M\"`)
(defpoll MONTH :interval "5s" `date +\"%B\"`)
(defpoll DAY :interval "5s" `date +\"%d  %a\"`)

(defwidget clock []
  (transform :rotate -25
             :transform-origin-x "50%"  
             :transform-origin-y "50%"  

    (box :orientation "h" :space-evenly false :halign "center"
      (label :class "clock-month"   :text MONTH :angle -90)
      (label :class "clock-hour"    :text HOUR :show-truncated false)
      (label :class "clock-divider" :text ":")
      (label :class "clock-min"     :text MIN :show-truncated false)
      (label :class "clock-month"   :text DAY :angle -90))))

(defwindow clock :stacking "bg" 
                 :monitor 0
                 :anchor "center right"
                 :focusable "false"
                 :geometry (geometry :x "77%" 
                                     :y "15%" 
                                     :width "100px"
                                     :height "700px")
    (clock))
